# ACTRA Obsidian 插件隐私说明（开发版）

更新日期：2026-09-23

本文件描述当前开发版插件的本地行为。生产发布前，ACTRA 仍需补充有法律效力的隐私政策、服务条款、数据处理区域、保留期限和删除服务等级承诺。

## 默认行为

- 插件默认不读取用户已有笔记。
- 插件默认不上传附件、不回写任务；未授权读取目录时不会读取或上传任何已有笔记。
- 插件只在 Obsidian 运行时工作，不尝试在应用关闭后访问 Vault。
- 插件不包含客户端遥测或广告 SDK。

## 连接数据

生产连接会打开 ACTRA OAuth 授权页，并向用户配置的 ACTRA HTTPS 服务发送公开 Client ID、回调地址、申请的 scope、随机 state 和一次性授权码。Access Token、Refresh Token 与临时 state 仅保存在 Obsidian `SecretStorage` 中；`data.json` 只保存 Secret ID、Token 有效期和最终授权范围。配对码仅用于不访问网络的本地演示模式，不会保存。

## 笔记数据

ACTRA 数据写入用户选择的 `writeRoot`。用户已有笔记只有在用户逐个授权读取目录后才可能被读取。目录授权确认后，插件立即上传一次，并默认在 Obsidian 运行时每 24 小时检查一次；用户可以关闭自动上传并只使用手动上传。插件使用本地内容哈希和修改时间避免重复发送未变化的文件。

从 ACTRA 拉取的 Markdown 会写入 `actra_managed` 元数据和 `actra-synced` 标签。upload 会在读取正文前通过元数据、标签及同步映射过滤这些文件，避免将 ACTRA 数据再次上传回 ACTRA。

插件不会上传本地绝对文件系统路径，不会因为笔记链接而读取授权目录外的目标文件。

## 撤权与删除

移除目录授权后，插件立即停止对该目录的后续读取和上传。关闭自动上传只停止 24 小时计划任务，用户仍可手动上传。当前 ACTRA 接口没有远端删除能力，因此此前已上传到 ACTRA 的副本不会由插件删除。

插件不会删除、清空或移入废纸篓中的任何 Obsidian 本地文件。解除 ACTRA 连接后，本地 Markdown 仍然保留。

## 日志与诊断

插件普通错误状态不包含 Token、配对码或笔记正文。用户主动导出的诊断文件包含插件版本、连接状态、Vault 相对授权路径、计数和已脱敏错误，不包含 Token、配对码、完整笔记正文或本地绝对路径。
